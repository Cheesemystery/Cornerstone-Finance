import Link from "next/link";import { SignInForm } from "@/components/sign-in-form";
export default async function SignInPage({searchParams}:{searchParams:Promise<{returnTo?:string}>}){const {returnTo}=await searchParams;return <main style={{padding:'clamp(28px,6vw,72px) 20px'}}><div style={{maxWidth:520,margin:'0 auto'}}><Link className="wordmark" href="/">Cornerstone</Link><div style={{margin:'52px 0 22px'}}><p className="eyebrow">Passwordless access</p><h1 style={{fontFamily:'var(--font-serif)',fontSize:'2.5rem',marginBottom:8}}>Sign in securely</h1><p className="muted">We will send a one-time link. Cornerstone never stores a password.</p></div><SignInForm returnTo={returnTo}/><p className="disclosure">You can continue with sample data without signing in.</p></div></main>}
