import Link from "next/link";
import { SignInForm } from "@/components/sign-in-form";
export default async function SignInPage({searchParams}:{searchParams:Promise<{returnTo?:string}>}){const {returnTo}=await searchParams;return <main className="entry-page"><div className="entry-panel"><Link className="wordmark" href="/">Cornerstone<span className="hello-dot">.</span></Link><header><p className="eyebrow">No password to remember</p><h1>Your space.<br/>One secure link.</h1><p>Account access uses an email link when available. The demo is open to everyone.</p></header><SignInForm returnTo={returnTo}/><Link href="/" className="demo-link workspace">← Back to Home</Link></div></main>}
