"use client";
import { ArrowUp } from "lucide-react";
import { FormEvent, useState } from "react";
export function AssistantClient(){const [question,setQuestion]=useState("");const [answer,setAnswer]=useState("Ask about a holding, a market event, or a financial concept. Cornerstone explains sourced facts and uncertainty—it does not tell you what to trade.");function submit(e:FormEvent){e.preventDefault();if(!question.trim())return;setAnswer(`A production answer to “${question.trim()}” will combine your structured portfolio facts with cited research. AI access is currently in safe fallback mode until a server key is configured.`);setQuestion("")}return <div className="workspace assistant-box"><div className="assistant-thread"><div className="assistant-message">{answer}</div></div><form className="assistant-composer" onSubmit={submit}><label className="sr-only" htmlFor="question">Ask Cornerstone</label><input id="question" value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Ask why a holding moved, or explain a concept…"/><button className="button primary" aria-label="Send question"><ArrowUp size={17}/></button></form><p className="disclosure">Answers are educational and may be incomplete. Verify material facts with the cited original source.</p></div>}
