"use client";
import { FormEvent, useState } from "react";
export function SignInForm({returnTo="/"}:{returnTo?:string}){const [email,setEmail]=useState("");const [status,setStatus]=useState("");async function submit(e:FormEvent){e.preventDefault();setStatus("Sending secure link…");const response=await fetch('/api/auth/magic-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,redirectTo:returnTo})});const body=await response.json();setStatus(response.ok?'Check your email for a secure sign-in link.':body.error||'Unable to send a link.')}return <form className="card" onSubmit={submit}><div className="form-field"><label htmlFor="email">Email address</label><input id="email" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><button className="button primary" type="submit">Email me a sign-in link</button>{status&&<p className="notice" role="status" style={{marginTop:16}}>{status}</p>}</form>}
